# KickLiveEmailNotifier
A local desktop app that runs in the background and sends email alerts when selected Kick streamers go live. Supports per-streamer notification toggles, manual or bulk streamer import, SMTP email setup, and Windows/macOS/Linux builds.
